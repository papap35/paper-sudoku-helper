# 開發流程準則

本文件說明本專案的開發、分支、commit 與 PR 慣例。

## 開發

- 先確認需求範圍，小步驟進行，避免一次性的大改動
- 修改前先閱讀相關檔案，理解現有結構與慣例，盡量沿用既有風格
- 不做需求外的重構、不新增未使用的功能或設定
- 完成後自行驗證（例如：語法檢查、啟動服務、呼叫關鍵 API/路由），再提交

## 建立分支 (Create Branch)

- 所有開發都在獨立分支上進行，不直接 commit 到 `main`
- 分支命名：`claude/<簡短描述>-<隨機ID>`，例如 `claude/repo-migration-continue-7e1m8e`
- 一個分支對應一個明確的功能或修正主題，避免混雜不相關的變更

## Commit

- 訊息聚焦在「為什麼」而不是「做了什麼」（程式碼本身已說明做了什麼）
- 格式：
  ```
  <簡短標題（中文，說明變更目的）>

  - <主要變更項目 1>
  - <主要變更項目 2>

  https://claude.ai/code/session_xxxxxxxx
  ```
- 每個 commit 盡量是一個邏輯完整的變更單位
- 一律建立新 commit，不對已推送的 commit 做 `amend`（除非使用者明確要求）
- 不使用 `--no-verify` 跳過 pre-commit hook；hook 失敗時先修正問題再建立新 commit

## 發布 PR

- base 分支為 `main`，head 為當前功能分支
- 標題簡短（70 字元內），用一句話說明變更內容
- 內文格式：
  ```markdown
  ## Summary
  - <變更摘要 1>
  - <變更摘要 2>

  ## Test plan
  - [x] <已完成的驗證項目>
  - [ ] <尚待驗證的項目>

  https://claude.ai/code/session_xxxxxxxx
  ```
- 只在使用者明確要求時才建立 PR

## 更新 PR

- 後續修正直接 push 新 commit 到同一分支，PR 會自動更新，不需重開 PR
- 除非分支剛建立且尚無其他人基於其上工作，否則不對已推送的歷史做 `rebase` 或 `force push`
- 收到 review 意見或 CI 失敗時：
  - 能直接判斷修法且不涉及架構性變更 → 直接修正並推送
  - 修法有歧義或涉及較大改動 → 先與使用者確認後再動手
- PR 合併或關閉前，視為流程尚未結束
